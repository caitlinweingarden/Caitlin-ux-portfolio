"use client";

/**
 * ThemeToggle - Editorial Neutralized
 * This component is kept to prevent import errors in the Navbar,
 * but it returns null so the button disappears from the UI.
 */
export default function ThemeToggle() {
  return null; 
}